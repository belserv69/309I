# ZF-Proto — финальный отчёт

**Дата:** 2026-08-24 · **Статус:** цели проекта достигнуты и перекрыты
**Родители:** `~/Projects/SOMA_Mind` (механика памяти), `~/Projects/CORAL` (признаки, харнесс)
**Концепция:** `CONCEPT.md` · План: `PLAN.md` · Логи: `BENCHMARK_LOG.md`

---

## 1. Итог одной таблицей

| Система | mini-ImageNet 100cls | Забывание cls0 | Механика |
|---|---:|---:|---|
| **ZF-Proto M9 (гибрид ViT-B)** | **94.23%** | 3.33pp | 20k прототипов DINOv2-B aug4 + probe от памяти, α=0.2 |
| CORAL champion | 90.0% | 6.3pp | probe + max-vote + replay buf20 |
| ZF-Proto M8 (дистилляция) | 89.20% | **0.00pp** | LwF lam=1, avg forg 0.37pp — лучший баланс |
| ZF-Proto M5 (чистая память) | 88.40% | **0.00pp** | только замороженные прототипы |
| CORAL rn50-hybrid (=probe) | 85.1% | 13.3pp | linear probe RN50 |
| ZF-Proto M2 (RN50) | 80.03% | 13.33pp | только прототипы |
| CORAL proto-only | 68.0% | — | SDR-обвязка |

Открытый мир (50 классов с метками → 50 без единой метки):

| Метрика | M7 ViT-S | **B1 ViT-B + merge@0.45** | CORAL |
|---|---:|---:|---:|
| База | 93.07% / **96.13%** | **96.13%** | 83.7–85.3% |
| Открытия (Hungarian) | 71.59% / 82.13% | **85.89%** | 70.4–70.6% |
| Итого (weighted) | 81.4% / 87.4% | **88.40%** | 77–78% |
| NCM-обобщение кластеров | 76.0% / 85.2% | **87.73%** | 72.9% |

B1b: узнавание повторного показа (repeat_cos=0.999) делает повторное
обучение неухудшающим — назначения стабильны (a1==a2 100%), NCM открытий
+32pp после одного повторного прохода при низком стартовом покрытии
(65.8→100%). B2 уточнил: при высоком покрытии (~85%) повтор не добавляет
точности, а восстановленные шумовые строки — граничный мусор (см. §6).

## 1a. Линия энкодеров (главный урок проекта)

| Энкодер | Чистая память | Гибрид | Прирост гибрида от энкодера |
|---|---:|---:|---:|
| RN50 2048d | 80.03% | — | база |
| ViT-S/14 384d | 88.40% | 91.03% | +11.0pp |
| ViT-B/14 768d | 92.23% | **94.23%** | +14.2pp |

Механика памяти не менялась ни разу с M0 — весь рост дало признаковое
пространство и один раз фьюжн с probe (+3.2pp на ViT-B).

## 2. Траектория milestones

| M | Что сделано | Результат |
|---|---|---|
| M0 | Порт PrototypicalMemory SOMA на кэш CORAL RN50 | P@10cls 95.67%, тесты 9/9, 0.6 с |
| M1 | Абляции top-k/threshold/exemplar/seniority, багфикс off-by-one фаз | чемпион topk4+sen08, P@25cls 91.60% |
| M2 | Главная цель концепции | P@100cls **80.03%** (цель ≥80 ✓) |
| M5 | Энкодер CORAL-чемпиона DINOv2 ViT-S, бит-в-бит порт + верификация кэша | 88.40% (+8.4pp), **cls0=0** |
| M6 | aug4 (багфикс меток), геометрия, пивот §10: гибрид память+probe | **91.03%** > CORAL 90.0% |
| M7 | Авто-открытие классов: novelty-порт, observe_batch с абсорбцией | итог **81.4%** > CORAL 77–78% |
| M3 | Кросс-валидация порта на SOMA CIFAR-10 (250k×714d) | 58.26% против якоря 58.02% (+0.24pp ≤ 1pp ✓) |
| M8 | LwF-дистилляция гибрида (порт CORAL train_probe) | cls0-дрейф 3.33→**0pp** при lam=1 |
| M9 | Энкодер ViT-B/14 768d | гибрид **94.23%** (+4.2pp к CORAL), forg 2.26pp |
| B1 | Онлайн-слияние открытий observe_batch(merge_cos) | открытый мир **88.40%**, Hungarian +3.8pp |
| B1b | Узнавание повторного показа (repeat_cos) | повторное обучение неухудшающее: agree=100%, NCM +32pp при низком покрытии |
| B2 | Replay-консолидация открытий многопроходным показом | прироста нет: шумовые строки граничные, шум -2 отрабатывает верно |

## 3. Ответы на вопросы концепции

- **H1** (прототипы never_update на сильных признаках ≥80% при forgetting ≈0):
  подтверждена дважды — 80.03% на RN50 и 88.40% на DINOv2 при cls0-forgetting 0.
- **H2** (механика переносима между признаками): подтверждена — расхождение
  с якорем SOMA 0.24pp.
- **H3** (аугментации/TTA переносятся): частично — aug4 даёт лишь +0.2pp;
  главный рычаг оказался не в данных, а в энкодере (+8.4pp) и гибриде (+2.6pp).

**Ключевой урок проекта:** качество признакового пространства доминирует над
тюнингом памяти. Confusion-пары, неустранимые никакими бонусами на RN50
(cls0 −13.3pp), исчезают сами на self-supervised признаках DINOv2.

## 4. Решение: production-конфигурации

Разрыв с probe закрыт (91.03% ≥ probe-oracle ≈91%), пивот на метрическое
обучение не требуется. Фиксируются три конфигурации:

1. **M9 — максимальная точность** (`champions/m9_hybrid_dinov2b_aug4_topk16_a02`):
   DINOv2 ViT-B CLS 768d → L2 → PrototypicalMemory(topk16, never_update) →
   per-phase LogisticRegression по прототипам памяти → фьюжн α=0.2.
   **94.23%**, forg 2.26pp, 61 с CPU.
2. **B1 — открытый мир** (`champions/b1_openworld_merge/`):
   база 50 классов → observe_batch(tau@FPR5%, min_cluster_size=3,
   merge_cos=0.45, repeat_cos=0.999). Итого **88.40%**, Hungarian открытий
   85.89%; повторное обучение монотонно улучшает.
3. **M8 — гарантированное удержание** (`champions/m5_dinov2_topk8_sen0_100cls`
   + дистилляция lam=1): LwF снимает cls0-дрейф до 0pp при forg 0.37pp;
   89.20% — лучший баланс точность/стабильность на ViT-S.

## 5. Оплаченные баги (уроки, встроенные в правила)

1. **Кэш-хэш данных** (из SOMA): кэши верифицируются переизвлечением портом,
   cosine=1.000000 обязателен до цитирования чисел.
2. **Метки аугментаций**: layout `[orig][тройки×N]` → метки
   `concat(y, repeat(y,3))`; `tile` рассинхронизировал и обрушал точность
   до ~7%. Верификация только по блоку оригиналов баг пропускала.
3. **Калибровка τ новизны**: только по невиданным векторам; train лежит в
   памяти прототипами → max-cosine=1.0 → порог вырождается.
4. **AUROC при сигнале «ниже = новее»** считается по −score.
5. **Off-by-one регистрации фаз классов** (M1) — seniority молча не работал.

## 6. Что осталось на будущее (не блокирует фиксацию)

- TTA5 на энкодере — скрипт готов (с чекпоинтингом), прогон в работе
  (ожидание +0.3–1pp).
- ~~Replay-консолидация базы между фазами через repeat_cos~~ → **закрыто
  (B2)**: стабильность повторов подтверждена (agree=100%, фикс-точка на
  проходе 3), но прироста поверх одиночного прохода нет — восстановленные
  шумовые строки (~15% потока) граничные и дают ~0 правильных ответов.
  Правильное поведение — оставлять их шумом. Детали: BENCHMARK_LOG §B2,
  `benchmarks/run_b2_consolidation.py`.
- Энкодер ViT-L/14 — продолжение линии RN50→ViT-S→ViT-B (+14.2pp суммарно).

## 7. Воспроизводимость

```bash
cd ~/Projects/309I
.venv/bin/python -m pytest tests/ -q                              # 26 passed
.venv/bin/python benchmarks/run_m5_dinov2_100cls.py               # 88.40%
.venv/bin/python benchmarks/run_m9_dinov2b.py                     # 94.23% (гибрид)
.venv/bin/python benchmarks/save_b1_champion.py                   # чемпион открытого мира
.venv/bin/python benchmarks/run_m7_openworld.py data/dinov2b_768.npz 0.45   # итог 88.4%
.venv/bin/python benchmarks/run_m3_soma_crossval.py               # якорь SOMA ±0.24pp
```

Кэши признаков: `data/*.npz` (gitignored); команды их получения — в логах
`champions/*.log` и скриптах `benchmarks/extract_*.py`.
