# ZF-Proto — финальный отчёт

**Дата:** 2026-08-23 · **Статус:** цели проекта достигнуты и перекрыты
**Родители:** `~/Projects/SOMA_Mind` (механика памяти), `~/Projects/CORAL` (признаки, харнесс)
**Концепция:** `CONCEPT.md` · План: `PLAN.md` · Логи: `BENCHMARK_LOG.md`

---

## 1. Итог одной таблицей

| Система | mini-ImageNet 100cls | Забывание cls0 | Механика |
|---|---:|---:|---|
| **ZF-Proto M6c (гибрид)** | **91.03%** | 10.00pp | 20k прототипов DINOv2 aug4 + probe от памяти, α=0.2 |
| CORAL champion | 90.0% | 6.3pp | probe + max-vote + replay buf20 |
| ZF-Proto M5 (чистая память) | 88.40% | **0.00pp** | только замороженные прототипы |
| CORAL rn50-hybrid (=probe) | 85.1% | 13.3pp | linear probe RN50 |
| ZF-Proto M2 (RN50) | 80.03% | 13.33pp | только прототипы |
| CORAL proto-only | 68.0% | — | SDR-обвязка |

Открытый мир (50 классов с метками → 50 без единой метки):

| Метрика | ZF-Proto M7 | CORAL |
|---|---:|---:|
| База | **93.07%** | 83.7–85.3% |
| Открытия (Hungarian) | **71.59%** | 70.4–70.6% |
| Итого | **81.4%** | 77–78% |
| AUROC новизны | **0.906** | 0.901* |
| NCM-обобщение кластеров | **76.0%** | 72.9% |

\* CORAL мерил ID vs CIFAR-10; ZF-Proto — ID vs невиданные классы того же датасета.

## 2. Траектория milestones

| M | Что сделано | Результат |
|---|---|---|
| M0 | Порт PrototypicalMemory SOMA на кэш CORAL RN50 | P@10cls 95.67%, тесты 9/9, 0.6 с |
| M1 | Абляции top-k/threshold/exemplar/seniority, багфикс off-by-one фаз | чемпион topk4+sen08, P@25cls 91.60% |
| M2 | Главная цель концепции | P@100cls **80.03%** (цель ≥80 ✓) |
| M5 | Энкодер CORAL-чемпиона DINOv2 ViT-S, бит-в-бит порт + верификация кэша | 88.40% (+8.4pp), **cls0=0** |
| M6 | aug4 (багфикс меток), геометрия, пивот §10: гибрид память+probe | **91.03%** > CORAL 90.0% |
| M7 | Авто-открытие классов: novelty-порт, observe_batch с абсорбцией | итог **81.4%** > CORAL 77–78% |
| M3 | Кросс-валидация порта на SOMA CIFAR-10 (250k×714d) | 58.26% против якоря 58.02% (+0.24pp ≤ 1pp ✓) |

## 3. Ответы на вопросы концепции

- **H1** (прототипы never_update на сильных признаках ≥80% при forgetting ≈0):
  подтверждена дважды — 80.03% на RN50 и 88.40% на DINOv2 при cls0-forgetting 0.
- **H2** (механика переносима между признаками): подтверждена — расхождение
  с якорем SOMA 0.24pp.
- **H3** (аугментации/TTA переносятся): частично — aug4 даёт лишь +0.2pp;
  главный рычаг оказался не в данных, а в энкодере (+8.4pp) и гибриде (+2.6pp).

**Ключевой урок проекта:** качество признакового пространства доминирует над
тюнингом памяти. Confusion-пары, неустранимые никакими бонусами на RN50
(cls0 −13.3pp), исчезают сами на self-supervised признаках DINOv2.

## 4. Решение: production-конфигурации

Разрыв с probe закрыт (91.03% ≥ probe-oracle ≈91%), пивот на метрическое
обучение не требуется. Фиксируются две конфигурации:

1. **M6c — максимальная точность** (`champions/m6c_hybrid_dinov2aug4_topk16_a02`):
   DINOv2 CLS 384d → L2 → PrototypicalMemory(topk16, never_update) →
   per-phase LogisticRegression по прототипам памяти → фьюжн α=0.2.
   91.03%, forg 3.56pp, 78 с CPU.
2. **M5 — гарантированное удержание** (`champions/m5_dinov2_topk8_sen0_100cls`):
   чистая память, ноль дрейфа старых классов. 88.40%, cls0=0.00pp, 7.5 с CPU.

## 5. Оплаченные баги (уроки, встроенные в правила)

1. **Кэш-хэш данных** (из SOMA): кэши верифицируются переизвлечением портом,
   cosine=1.000000 обязателен до цитирования чисел.
2. **Метки аугментаций**: layout `[orig][тройки×N]` → метки
   `concat(y, repeat(y,3))`; `tile` рассинхронизировал и обрушал точность
   до ~7%. Верификация только по блоку оригиналов баг пропускала.
3. **Калибровка τ новизны**: только по невиданным векторам; train лежит в
   памяти прототипами → max-cosine=1.0 → порог вырождается.
4. **AUROC при сигнале «ниже = новее»** считается по −score.
5. **Off-by-one регистрации фаз классов** (M1) — seniority молча не работал.

## 6. Что осталось на будущее (не блокирует фиксацию)

- Перефрагментация открытий: 93 кластера на 50 классов; слияние в момент
  наблюдения вместо пост-хока.
- TTA5 на энкодере — не проверено (ожидание +0.3–1pp).
- ViT-B/14 (768d) — прямое продолжение линии энкодеров.
- Дистилляция гибрида в чистую память (снять cls0-дрейф probe).

## 7. Воспроизводимость

```bash
cd ~/Projects/309I
.venv/bin/python -m pytest tests/ -q                 # 25 passed
.venv/bin/python benchmarks/run_m5_dinov2_100cls.py  # 88.40%
.venv/bin/python benchmarks/run_m6c_hybrid.py        # 91.03% (α=0.2)
.venv/bin/python benchmarks/run_m7_openworld.py      # итог 81.4%
.venv/bin/python benchmarks/run_m3_soma_crossval.py  # якорь SOMA ±0.24pp
```

Кэши признаков: `data/*.npz` (gitignored); команды их получения — в логах
`champions/*.log` и скриптах `benchmarks/extract_*.py`.
